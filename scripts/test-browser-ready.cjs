const {test}=require('node:test');const assert=require('node:assert/strict');const fs=require('node:fs');const vm=require('node:vm');
function harness(){const listeners={};let calls=0;const context={URLSearchParams,location:{hash:'#keshi-launch='+'a'.repeat(43),pathname:'/',search:''},history:{replaceState(){}},fetch:()=>{calls++;return Promise.resolve()},document:{addEventListener:(n,f)=>listeners[n]=f},addEventListener:(n,f)=>listeners[n]=f};context.window=context;vm.createContext(context);vm.runInContext(fs.readFileSync(require('node:path').join(__dirname,'../frontend/browser-compat.js'),'utf8'),context);return{context,listeners,calls:()=>calls}}
test('DOMContentLoaded alone is not proof of working application',()=>{const h=harness();h.listeners.DOMContentLoaded();assert.equal(h.calls(),0)});
test('working application acknowledges only after readiness',()=>{const h=harness();h.listeners.DOMContentLoaded();h.context.keshiUiReady=true;h.listeners['keshi-ui-ready']();assert.equal(h.calls(),1)});
test('script failure prevents a misleading successful acknowledgement',()=>{const h=harness();h.context.keshiUiReady=true;h.context.keshiUiFailed=true;h.listeners.DOMContentLoaded();assert.equal(h.calls(),0)});
